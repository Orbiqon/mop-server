= form_with(model: colour, local: true) do |form|
  - if colour.errors.any?
    #error_explanation
      %h2
        = pluralize(colour.errors.count, "error")
        prohibited this colour from being saved:
      %ul
        - colour.errors.full_messages.each do |message|
          %li= message
  .form-group
    = form.label :name
    = form.text_field :name, class: "form-control"
  .form-group
    .form-check.form-switch
      = form.check_box :status, class: "form-check-input", id: "flexSwitchCheckDefault"
      %label.form-check-label{for: "flexSwitchCheckDefault"} Active
  .actions
    = form.submit class: "btn btn-primary"